{
  "name": "jellyfin-plugin-sso",
  "version": "1.0.0",
  "description": "SSO Plugin for Jellyfin",
  "private": true,
  "scripts": {
    "build": "node build-qr-lib.js",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "qr-code-styling": "^1.9.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
